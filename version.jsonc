{
  // SilkyNvg version configuration
  // Used by ComputeGitVersion target in SilkyNvg.Build.props to compute package version
  // Format: {version}.{buildNumberOffset}-{GitHeight}.{LocalBuildNum}.g{GitHash}
  // Example: 1.0.7-42.0.g3be210cd
  "version": "1.0",
  "buildNumberOffset": 13
}